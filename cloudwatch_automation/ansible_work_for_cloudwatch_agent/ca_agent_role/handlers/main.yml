---
# handlers file for CloudWatch Agent role

- name: Restart CloudWatch Agent
  ansible.builtin.systemd_service:
    name: "{{ cloudwatch_agent.service_name }}"
    state: restarted
    daemon_reload: true
